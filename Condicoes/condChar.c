#include <stdio.h>
#include <stdlib.h>

int main()
{

    char letra = 'x';

    if (letra == 'x')
    {
        printf("a letra é x \n");
    }

    printf("O codigo ASCII da letra x é: %d \n", letra);

    if (letra == 120)
    {

        printf("a letra é x \n");
    }

    return 0;
}